# PodClean Configuration

# Base URL for RSS feed generation
# Set this to your server's IP/hostname and port
# Example: http://192.168.1.50:8080
BASE_URL=http://localhost:8080

# Cron schedule for automatic processing
# Default: 2am daily
SCHEDULE=0 2 * * *

# Timezone (for accurate scheduling)
# See: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=America/New_York

# Ollama settings for LLM-based ad detection
# Set to the Ollama service URL (default uses Docker service name)
OLLAMA_URL=http://ollama:11434

# Model to use for ad detection (smaller = faster on CPU)
# Options: llama3.2:3b (recommended for CPU), llama3.2:1b (faster), mistral:7b (more accurate)
OLLAMA_MODEL=llama3.2:3b

# Enable/disable LLM-based ad detection (falls back to pattern matching if disabled)
USE_LLM_DETECTION=true
